import React from "react";
import Logo from "components/layouts/Logo";

const Workshop = () => {
  return (
    <div className="page">
      <Logo name="workshop" />
    </div>
  );
};

export default Workshop;
