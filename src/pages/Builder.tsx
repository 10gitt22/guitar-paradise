import React from "react";
import Logo from "components/layouts/Logo";

const Builder = () => {
  return (
    <div className="page">
      <Logo name="builder" />
    </div>
  );
};

export default Builder;
